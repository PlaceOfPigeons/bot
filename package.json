{
  "name": "your-project-name",
  "version": "1.0.0",
  "scripts": {
    "start": "webpack serve",  // Команда для запуска разработки (если используется Webpack или подобный инструмент)
    "build": "webpack --mode production",  // Команда для сборки проекта
    "deploy": "firebase deploy --only hosting",  // Команда для деплоя на Firebase
    "test": "echo \"Error: no test specified\" && exit 1"  // Тесты, если они нужны
  },
  "dependencies": {
    "firebase": "^9.0.0",  // Убедитесь, что Firebase добавлен в зависимости
    "react": "^17.0.0",  // Если используете React
    "react-dom": "^17.0.0",  // Если используете React
    "webpack": "^5.0.0",  // Если используете Webpack
    "webpack-cli": "^4.0.0"  // Для Webpack
  },
  "devDependencies": {
    "webpack-dev-server": "^4.0.0"  // Для разработки с Webpack
  },
  "engines": {
    "node": "16"  // Указываем, что используем Node 16, если нужно
  }
}
